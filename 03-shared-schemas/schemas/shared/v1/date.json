{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.company.com/shared/v1/date.json",
  "title": "Date",
  "description": "A calendar date with no time and no zone (RFC 3339 full-date). Use for birthdays, due dates, accounting periods: things that are the same date everywhere.",
  "x-lifecycle": "active",
  "type": "string",
  "format": "date",
  "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$",
  "examples": ["2026-09-20"]
}
