# Prop. VII.9 - Identifiers are ULIDs generated by the system of record

| | |
|---|---|
| **Level** | MUST |
| **Status** | Draft |
| **Since** | 2026-09-20 |
| **Owner** | Principal engineers |
| **Supersedes** | - |

## Statement

The identifier (Def. I.18) of every aggregate MUST be a ULID conforming to the shared Identifier schema (Prop. IV.2) and MUST be generated by the aggregate's system of record (Def. VII.1) at creation. Read models MUST carry the identifier unchanged and MUST NOT issue identifiers of their own for the concept. Database surrogate keys, if any, MUST NOT appear in any contract.

## Given

* Def. I.18, Def. VII.1, Def. VII.3
* Post. I.4, Post. VII.1
* Prop. II.8, Prop. IV.2, Prop. VII.2
* CN 3, CN 5

## Demonstration

Post. VII.1 gives each aggregate exactly one authority for its state; identity is the first fact about an aggregate, so the same authority mints it. A ULID (Def. I.18, Prop. IV.2) can be generated without coordination and sorts by creation time, so the system of record can assign it before commit and expose it in the creation response and event, which lets a retried creation under Post. I.4 be recognised (Prop. II.8). Read models are copies (Def. VII.3) and by Prop. VII.2 never authoritative, so an identifier minted there would name nothing. By CN 5 one concept has one identifier shape, and by CN 3 an integer key that leaks into a response becomes a contract nobody intended. ∎ Q.E.D.

## Corollaries

* **Cor. VII.9.1** - A client MAY propose an identifier only where the contract declares it (Prop. II.8 idempotent create); the system of record still validates and owns it.
* **Cor. VII.9.2** - Natural keys (email, account number) are attributes, never identifiers; they may change and identifiers may not.

## Construction

* `Company.Contracts.Shared` `Identifier` type and ULID generator (Prop. IV.2); no other ULID library in service code.
* Relational: `CHAR(26)` or `BYTEA(16)`/`BINARY(16)` primary key holding the ULID; a clustered index on it benefits from time ordering. DynamoDB: ULID as partition key or sort key as the access pattern requires.
* Internal surrogate keys, if present for storage reasons, mapped in the persistence layer and excluded from DTOs by an architecture test.

## Conformance

OpenAPI linter rule (Prop. II.1 ruleset) requiring every `id` property to reference the shared Identifier schema; architecture test that no type in the contracts assembly exposes an integer or GUID property named `*Id`.

## Scholium

GUIDs would satisfy uniqueness but not ordering; integers would satisfy ordering but not independence. The ULID is chosen in Book IV; this proposition only fixes who mints it.
